[build]
builder = "dockerfile"
dockerfilePath = "../Dockerfile"

[deploy]
startCommand = "gunicorn -w 1 -b 0.0.0.0:$PORT --timeout 120 main:app"
healthcheckPath = "/ping"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
